# Notice on the older Pratt package

The uploaded archive `ntable-python-py38-pratt-baseline(2).zip` contained an earlier
D-only RQ3 reuse experiment. That older RQ3 implementation is intentionally **not**
included in this reviewer package because it does not correspond to the manuscript's
current shared-topology reuse method.

The authoritative Pratt baseline used with the current RQ3 experiment is:

`core_rq3/src/ntable/pratt.py`

The current shared-topology reuse implementation is:

`core_rq3/src/ntable/reuse.py`
